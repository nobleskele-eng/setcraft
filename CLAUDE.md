This is a UI-only task. Do not change any copy, labels, field names, values, or content. Do not invent stats, names, or placeholder facts. If a slot needs new text, output [NEEDS COPY] and tell me. Do not claim anything works unless you have actually run it. Show me the diff before applying. All color, type, spacing, and radius values come from tokens.css. Never write a raw hex, font name, px spacing value, or border-radius in a component. If a needed token doesn't exist, stop and tell me rather than inventing one.